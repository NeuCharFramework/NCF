@page "{handler?}"
@addTagHelper *, Senparc.Areas.Admin
@model Senparc.Areas.Admin.Areas.Admin.Pages.SystemConfig_IndexModel
@{
    ViewData["Title"] = AR["SystemConfig.Title"];
    Layout = "_Layout_Vue";
}

@section Style{
    <link href="~/css/Admin/SystemConfig/Index.css" rel="stylesheet" />
}

@section breadcrumbs {
    <el-breadcrumb-item>@AR["Common.系统管理"]</el-breadcrumb-item>
    <el-breadcrumb-item>@AR["SystemConfig.Title"]</el-breadcrumb-item>
}
<div>
    <div class="admin-systemconfig-info">
        <h2 class="system-info">@AR["SystemConfig.BasicInfo"]</h2>

        <el-table :data="tableData"
                  border
                  style="width: 100%">
            <el-table-column align="center"
                             label="@AR["SystemConfig.SystemName"]">
                <template slot-scope="scope">
                    {{ scope.row.systemName}}
                </template>
            </el-table-column>
            <el-table-column align="center"
                             label="@AR["SystemConfig.PublishMode"]">
                <template slot-scope="scope">
                    {{ scope.row.hideModuleManager }}
                </template>
            </el-table-column>
            <el-table-column align="center" label="Footer" min-width="240">
                <template slot-scope="scope">
                    <span class="system-footer-preview" v-text="scope.row.footerContent"></span>
                </template>
            </el-table-column>
            <el-table-column label="@AR["Common.操作"]" align="center">
                <template slot-scope="scope">
                    <el-button size="mini"
                               type="primary"
                               v-has="['admin-edit']"
                               @@click="handleEdit(scope.$index, scope.row)">@AR["Common.编辑"]</el-button>
                </template>
            </el-table-column>
        </el-table>
        @*<pagination :total="paginationQuery.total"
            :page.sync="listQuery.pageIndex"
            :limit.sync="listQuery.pageSize"
            @@pagination="getList"></pagination>*@
        <!--编辑、新增 -->
        <el-dialog :title="dialog.title" :visible.sync="dialog.visible" :close-on-click-modal="false">
            <el-form ref="dataForm"
                     :rules="dialog.rules"
                     :model="dialog.data"
                     label-position="left"
                     label-width="100px"
                     style="max-width: 400px; margin-left:50px;">
                @*<p class="username-tip" v-if="dialog.title==='编辑管理员'">不建议修改用户名！</p>*@
                <el-form-item label="@AR["SystemConfig.SystemName"]" prop="systemName">
                    <el-input v-model="dialog.data.systemName" clearable placeholder="@AR["SystemConfig.SystemName"]" />
                </el-form-item>
                <el-form-item label="Footer" prop="footerContent">
                    <el-input
                        v-model="dialog.data.footerContent"
                        type="textarea"
                        :rows="4"
                        maxlength="2000"
                        show-word-limit
                        placeholder="© @DateTime.Now.Year Senparc">
                    </el-input>
                    <div class="system-footer-help">支持普通文本和绝对 http/https 链接的 &lt;a&gt; 标签；其他 HTML 会被编码。</div>
                </el-form-item>
            </el-form>
            <div slot="footer" class="dialog-footer">
                <el-button @@click="dialog.visible = false">@AR["Common.取消"]</el-button>
                <el-button :loading="dialog.updateLoading" type="primary" @@click="updateData">@AR["Common.确认"]</el-button>
            </div>
        </el-dialog>
    </div>

</div>
@section scripts{
    <script src="~/js/Admin/Pages/SystemConfig/Index.js"></script>
}
